from skbuild import setup

setup(
    name="cugrad",
    version="0.0.1",
    # Optionally include author, description, etc.
    # No extension modules need to be specified here;
    # scikit-build will use CMakeLists.txt to find and build them.
)
